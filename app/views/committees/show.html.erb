<div class="page-header">
  <h1><%= @committee.name %> <small class="pull-right"><%= @committee.external_id %></small></h1>
</div>

<%= render 'shared/representatives_table', locals: { representatives: @committee.representatives, show_party: true } %>

<h2><%= Issue.model_name.human(count: 2).capitalize %></h2>
<%= render 'shared/issue_table', locals: { issues: @committee.issues } %>


