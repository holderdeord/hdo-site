<div class="page-header">
  <h1><%= District.model_name.human :count => 2 %></h1>
</div>

<p class="pull-right">Viser <strong><%= @districts.size %></strong> fylker.</p>

<table class="table table-striped">
  <thead>
    <tr>
      <th><%= District.human_attribute_name :external_id %></th>
      <th><%= District.human_attribute_name :name %></th>
    </tr>
  </thead>

  <tbody>
      <% @districts.each do |com| %>
      <tr>
        <td><%= com.id %></td>
        <td><%= link_to com.name, com %></td>
      </tr>
      <% end %>
  </tbody>
</table>
