<aside class="span4 pull-right qa-form-help">
  <p>Bli med som testbruker av Holder de ords spørsmål- og svar-tjeneste!</p>

  <p>Her kan du enkelt spørre dine representanter på Stortinget om sakene som opptar deg, hvorfor de har stemt på en bestemt måte, eller hvordan de skal følge opp et løfte.</p>

  <p>Spørsmål og svar godkjennes av Holder de ord, og må være i tråd med <a href="#" data-toggle="modal" data-target="#code-of-conduct">våre retningslinjer</a>.</p>
</aside>

<div class="span7">
  <%= form_for(@question) do |f| %>
    <% if @question.errors.any? %>
      <div class="alert alert-error">
        <ul>
          <% @question.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="control-group">
      <div class="controls">
        <label class="control-label"><%= Question.human_attribute_name :representative %></label>
        <div class="chosen-xlarge">
          <%= f.select :representative, representative_options, include_blank: 'Velg en representant...' %>
        </div>
      </div>
    </div>

    <div class="control-group">
      <div class="controls">
        <label class="control-label"><%= Question.human_attribute_name :body %></label>
        <%= f.text_area :body, class: 'span12 qa-form-body',
                               rows:  5,
                               placeholder: "Kjære parti/representant! Hvordan kan det ha seg at..." %>
      </div>
    </div>

    <div class="row-fluid">
      <div class="control-group span5">
        <label class="control-label"><%= Question.human_attribute_name :from_name %></label>
        <div class="controls">
          <%= f.text_field :from_name, class: 'span12' %>
        </div>
      </div>

      <div class="control-group span5 offset2">
        <label class="control-label"><%= Question.human_attribute_name :from_email %></label>
        <div class="controls">
          <%= f.text_field :from_email, class: 'span12' %>
        </div>
      </div>
    </div>

    <div class="control-group">
       <div class="controls">
         <%= f.submit class: 'btn btn-primary', value: 'Send' %>
       </div>
    </div>
  <% end %>
</div>

<div id="code-of-conduct" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="code-of-conduct" aria-hidden="true">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h3>Retningslinjer</h3>
  </div>

  <div class="modal-body">
    <%= render partial: 'conduct' %>
  </div>

  <div class="modal-footer">
     <button class="btn" data-dismiss="modal" aria-hidden="true">Skjønner!</button>
   </div>
</div>

<% content_for :document_ready do %>
  <script type="text/javascript" charset="utf-8">
    $(document).ready(function() {
      $("#question_representative").chosen();
    });
  </script>
<% end %>