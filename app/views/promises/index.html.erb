<% page_title "#{Promise.model_name.human(count: 2).capitalize} - #{t 'app.title'}" %>
<div class="page-header">
  <h1>
    <%= Promise.model_name.human(count: 2).capitalize %>
    <small class="hidden-phone pull-right"><%= @promises.count %> løfter fra valgprogrammene 2009-2013</small>
  </h1>
</div>

<!--<div class="alert alert-info">
  Les mer om <%= link_to 'vår løftedatabase', home_about_path(anchor: 'valglfter-og-lftedatabasen')  %>.
</div>-->

<div class="row-fluid">
  <%= render partial: 'shared/promises_navigation', locals: { categories: @categories, parties: @parties } %>
</div>

<%= render partial: 'shared/promises_results' %>

<% content_for :document_ready do %>
  <script>
  /*
    $(document).ready(function () {
      var widget = HDO.promiseWidget.create({
        targetEl: $(".promises-results"),
        categoriesSelector: $(".categories"),
        partiesSelector: $(".party-nav"),
        subCategoriesSelector: $("#subcategory-dropdown"),
        server: HDO.promiseServerFacade,
        activeParty: $(".show-all-active").get(0),
        spinnerEl: $("#spinner")
      });

      widget.init();
    });
*/
  </script>
<% end %>