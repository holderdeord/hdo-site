<div class="page-header">
  <h1><%= Vote.model_name.human(count: 2).capitalize %></h1>
</div>

<%= render partial: "shared/votes_summary_table", locals: {votes: @votes, show_issues: true } %>
<%= will_paginate @votes %>