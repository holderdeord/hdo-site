<% @groups.each do |tag, issues| %>
<div class="row-fluid">
  <h3><%= tag %></h3>
  <ul>
    <% issues.each do |issue| %>
    <li><%= link_to issue.title, issue %></li>
    <% end %>
  </ul>
</div>
<% end %>