<aside class="muted">
  <%= Issue.human_attribute_name(:updated_at) %> <%= l issue.updated_at, format: :text %>.

  <span class="issue-tags">
    <% issue.tags.each do |tag| %>
      <%= link_to tag.name, issues_path(anchor: "tag-#{tag.slug}") %>
    <% end %>
  </span>
</aside>
