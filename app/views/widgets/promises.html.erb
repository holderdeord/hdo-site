<header>
  <h1><%= Promise.model_name.human(count: 2).capitalize %></h1>
</header>

<article>
  <% @promise_groups.each do |parties, promises| %>
    <h2 class="party <%= slug_for_parties(parties) %>">
      <%# TODO: THE LINK(S) BELOW SHOULD LEAD TO THE PARTY PAGE ON HDO %>
      <a href="#"><%= parties.map(&:name).to_sentence %></a>
    </h2>
    <ul>
      <% promises.each do |promise| %>
      <li><%= promise.body %></li>
      <% end %>
    </ul>
  <% end %>
</article>
