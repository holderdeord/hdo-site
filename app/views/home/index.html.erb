<% content_for :jumbotron do %>
  <p>
    Din uavhengige kilde for fakta om stortingspolitikk. Holder de ord gjør det enkelt å sjekke hva politikerne på Stortinget stemmer og lover i saker du er opptatt av.
    <%= link_to 'Les mer', home_about_path %>
  </p>
<% end %>

<% if AppConfig.questions_enabled %>
  <div class="row-fluid promoboxes">
    <div class="span6 promoted">
      <img class="promoimage" src="<%= asset_path('promoarea/promo_question.png') %>" alt="Spørsmålsgrafikk">
      <h4><%= t 'app.questions.title' %></h4>
      <p>Nå kan du stille spørsmål til en stortingspolitiker om saker som opptar deg. Bli med og test vår nye tjeneste.</p>
      <p>
        <%= link_to t('app.questions.new'), new_question_path, class: 'btn btn-primary' %>
      </p>
    </div>
    <div class="span6">
      <img class="promoimage" src="<%= asset_path('promoarea/promo_money.png') %>" alt="Pengegrafikk">
      <h4>Støtt vårt arbeid</h4>
      <p>
        Send kodeord HOLDERDEORD til 2160 for å støtte oss med 150 kr. Vi tar også i mot større gaver og sponsorbidrag.
        <span class="read-more"><%= link_to t('app.read_more'), home_support_path %></span>
      </p>
    </div>
  </div>
<% end %>

<div class="row">
  <div class="span7">
    <h3>Aktuelle saker</h3>

    <section class="top-issues">
      <% @issues.each_slice(2) do |issues| %>
        <div class="row-fluid">
          <% issues.each do |issue| %>
            <article class="issue span6">
              <%= link_to issue do %>
                <h4><%= issue.title %></h4>
                <p>
                  <%= awesome_truncate issue.description, 200 %>
                  <span class="read-more"><%= t('app.read_more') %></span>
                </p>
              <% end %>
            </article>
          <% end %>
        </div>
      <% end %>
    </section>
  </div>

  <div class="span4 blog">
    <h3>Blogger de ord?</h3>

    <div>
      <h4><%= link_to @latest_post.title, @latest_post.url %></h4>
      <small>Skrevet av: <%= @latest_post.author %></small>
      <% @latest_post.text.each do |paragraph| %>
        <% if (!paragraph.to_s.include? "<br>") %>
          <p><%= paragraph.to_s.html_safe %><p>
        <% end %>
      <% end %>
      <%= link_to "Les hele innlegget", @latest_post.url %>
    </div>
  </div>

</div>



<% if AppConfig.leaderboard_enabled %>
  <%= render partial: 'leaderboard' %>
<% end %>

<h3 class="lead">Alle tema</h3>
<% @all_tags.each_slice(4) do |tags| %>
  <div class="row-fluid">
  <% tags.each do |tag| %>
    <div class="span3">
      <%= link_to tag.name, issues_path(anchor: "tag-#{tag.slug}") %>
    </div>
  <% end %>
  </div>
<% end %>

<hr class="soften">

<% unless AppConfig.leaderboard_enabled %>
  <h3 align="center">SJEKK DITT PARTI</h3>

  <div class="row-fluid">
    <div class="home-party-list">
      <% @parties.each do |party| %>
        <%= link_to party do %>
        <div class="home-party-logo">
          <%= image_tag party.logo, alt: party.name, width: '290', height: '290' %>
        </div>
        <% end %>
      <% end %>
    </div>
  </div>
<% end %>

<hr class="soften">
