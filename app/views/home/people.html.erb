<% page_title "Kontakt - #{t('app.about')} - #{t('app.title')}" %>
<div class="page-header">
  <h1>Kontakt oss</h1>
</div>

<div class="row-fluid">
  <div class="span2"><%= image_tag 'hdo/daniel.jpg', class: 'home-photo' %></div>
  <div class="span4">
    <h2>Daniel Rees <br><small>daglig leder</small></h2>
    <dl>
      <dt>Epost</dt>
      <dd><a href="mailto:<%= obscure_email 'daniel@holderdeord.no' %>"><%= obscure_email 'daniel@holderdeord.no' %></a></dd>
      <dt>Telefon</dt>
      <dd><a href="tel:+4799452400"></a>+47 99452400</dd>
    </dl>
  </div>
  <div class="span2"><%= image_tag 'hdo/tiina.jpg', class: 'home-photo' %></div>
  <div class="span4">
    <h2>Tiina Ruohonen <br><small>kommunikasjons- og pressesjef</small></h2>
    <dl>
      <dt>Epost</dt>
      <dd><a href="mailto:<%= obscure_email 'tiina@holderdeord.no' %>"><%= obscure_email 'tiina@holderdeord.no' %></a></dd>
      <dt>Telefon</dt>
      <dd><a href="tel:+4790983161"></a>+47 90983161</dd>
    </dl>
  </div>
</div>
<h3>Post- og besøksadresse</h3>
<dl>
  <dt>Holder de ord</dt>
  <dd>c/o <a href="http://www.meshnorway.com/">Mesh</a></dd>
  <dd>Tordenskioldsgate 3</dd>
  <dd>0160 Oslo</dd>
  <dd>Norge</dd>
</dl>
<hr class="soften">

<% page_title "#{t('app.people')} - #{t('app.about')} - #{t('app.title')}" %>

<h1><%= t('app.people') %></h1>

<% @all_members.each do |person| %>
  <div class="person">
      <%= image_tag "#{gravatar_url(person.email)}", class: 'photo'%>
      <span class="name"><%= person.name %></span>
      <div class="muted bio"><%=h person.title %></div>
  </div>
<% end %>
<br>

<h3>Styre</h3>

<% @board.each do |person| %>

  <div class="person">
    <%= image_tag gravatar_url(person.email), class: 'photo'%>

    <% if person.email %>
      <a class="name" href="mailto:<%= obscure_email person.email %>">
        <%= person.name %>
      </a>
    <% else %>
      <span class="name">
        <%= person.name %>
      </span>
    <% end %>

    <div class="bio">
      <%= person.description.try(:html_safe) %>
    </div>

  </div>
<% end %>

<br>

<h3>Andre bidragsytere</h3>
<% @alumni.each do |person| %>
  <div class="bidragsyter">
    <%= person.name %>
  </div>
<% end %>
