Kjære <%= @resource.name %>

Du får denne mailen fordi du har bedt om å få et nytt passord på holderdeord.no.

Følg <%= edit_password_url(@resource, reset_password_token: @resource.reset_password_token, protocol: AppConfig.ssl_enabled ? 'https' : 'http') %> for å sette nytt passord og logge inn.

Hvis det ikke er du som har bedt om nytt passord, er det bare å overse denne mailen. Kontakt oss på <%= AppConfig.question_answer_email %> hvis du har noen spørsmål.

Ha en god dag!

Vennlig hilsen,
Holder de ord