<h3><%= ParliamentIssue.model_name.human(count: 2).capitalize %></h3>

<ul>
  <% results.sort_by { |r| r.last_update.to_date }.reverse.each do |r| %>
    <li><%= link_to r.summary, parliament_issue_path(r.slug) %></li>
  <% end %>
</ul>
